interface:
  display_name: "Destroy Theory"
  short_description: "Find counterexamples and proof gaps"
  default_prompt: "Use $destroy-theory to look for counterexamples, proof gaps, and the true boundary of this claim."

policy:
  allow_implicit_invocation: true
