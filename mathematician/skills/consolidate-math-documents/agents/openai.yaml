interface:
  display_name: "Consolidate Math Documents"
  short_description: "Merge mathematics into one canonical target"
  default_prompt: "Use $consolidate-math-documents to merge these mathematical documents into one self-contained canonical target."

policy:
  allow_implicit_invocation: false
