interface:
  display_name: "Audit Assumptions"
  short_description: "Trace what each hypothesis is doing"
  default_prompt: "Use $audit-assumptions to explain which assumptions shape the statement, the proof, and the theorem."

policy:
  allow_implicit_invocation: true
