interface:
  display_name: "Write Proof Exposition"
  short_description: "Reconstruct complete proofs across specialties"
  default_prompt: "Use $write-proof-exposition to reconstruct this established proof as a self-contained account for mathematicians outside the specialty."

policy:
  allow_implicit_invocation: false
